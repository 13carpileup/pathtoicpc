export default function About() {
  return (
    <section className="page-section">
      <p className="eyebrow">About</p>
      <h1>A simple multipage React app.</h1>
      <p className="lead">
        React Router handles navigation between pages without a full browser
        reload. Add more pages under <code>src/pages</code> and register them in
        <code>src/App.jsx</code>.
      </p>
    </section>
  );
}
